<Window x:Name="Main" x:Class="E.Player.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:E.Player"
        mc:Ignorable="d"
        Title="E Player" Height="500" Width="800" MinHeight="400" PreviewKeyDown="Main_PreviewKeyDown" AllowDrop="True" Loaded="Main_Loaded" Closing="Main_Closing" SizeChanged="Main_SizeChanged" StateChanged="Main_StateChanged" Icon="图片/E Player.ico" MinWidth="500">

    <!--自定义控件模板-->
    <Window.Resources>
        <!--颜色-->
        <SolidColorBrush x:Key="一级字体颜色" Color="Black" />
        <SolidColorBrush x:Key="二级字体颜色" Color="#FF3C3C3C" />
        <SolidColorBrush x:Key="三级字体颜色" Color="#FF787878" />
        <SolidColorBrush x:Key="一级背景颜色" Color="Gainsboro" />
        <SolidColorBrush x:Key="二级背景颜色" Color="#FFF0F0F0" />
        <SolidColorBrush x:Key="三级背景颜色" Color="White" />
        <SolidColorBrush x:Key="一级边框颜色" Color="#FFA0A0A0" />
        <SolidColorBrush x:Key="有焦点选中颜色" Color="#7F007FFF"/>
        <SolidColorBrush x:Key="无焦点选中颜色" Color="#7F7FC8FF"/>

        <!--按钮样式-->
        <Style x:Key="菜单按钮样式" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource 一级字体颜色}"/>
            <Setter Property="Background" Value="{DynamicResource 一级背景颜色}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource 一级边框颜色}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
                            <TextBlock x:Name="text" Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderThickness" Value="4,0,0,0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <!--<Setter TargetName="border" Property="BorderThickness" Value="6,0,0,0"/>-->
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="text" Property="Foreground" Value="{DynamicResource 三级字体颜色}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="工具按钮样式" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource 一级字体颜色}"/>
            <Setter Property="Background" Value="{DynamicResource 二级背景颜色}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Width" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
                            <TextBlock x:Name="text" Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource 一级背景颜色}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <!--<Setter TargetName="border" Property="Background" Value="{DynamicResource 一级背景颜色}"/>-->
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="text" Property="Foreground" Value="{DynamicResource 三级字体颜色}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="自由按钮样式" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource 一级字体颜色}"/>
            <Setter Property="Background" Value="{DynamicResource 二级背景颜色}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" SnapsToDevicePixels="True">
                            <TextBlock x:Name="text" Text="{TemplateBinding Content}" Foreground="{TemplateBinding Foreground}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource 一级背景颜色}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <!--<Setter TargetName="border" Property="Background" Value="{DynamicResource 一级背景颜色}"/>-->
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter TargetName="text" Property="Foreground" Value="{DynamicResource 三级字体颜色}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--标签样式-->
        <Style x:Key="标题标签样式" TargetType="Label">
            <Setter Property="Foreground" Value="{DynamicResource 一级字体颜色}"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="自由标签样式" TargetType="Label">
            <Setter Property="Foreground" Value="{DynamicResource 一级字体颜色}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!--文本显示框样式-->
        <Style x:Key="文本显示框样式" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource 二级字体颜色}"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="3"/>
        </Style>
        
        <!--流式面板样式-->
        <Style x:Key="水平流式面板样式" TargetType="WrapPanel">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
            <Setter Property="ItemWidth" Value="60"/>
            <Setter Property="ItemHeight" Value="30"/>
        </Style>
    </Window.Resources>

    <Grid x:Name="MainGrid" >
        <DockPanel>
            <!--左-->
            <DockPanel x:Name="PanLeft" Background="{DynamicResource 二级背景颜色}" >
                <StackPanel DockPanel.Dock="Top">
                    <Button x:Name="BtnFold" Content="折叠" Style="{DynamicResource 菜单按钮样式}" BorderThickness="4,0,0,0"/>
                    <Button x:Name="BtnFile" Content="文件" Style="{DynamicResource 菜单按钮样式}" BorderThickness="4,0,0,0"/>
                    <Button x:Name="BtnControl" Content="控制" Style="{DynamicResource 菜单按钮样式}"/>
                    <Button x:Name="BtnSetting" Content="设置" Style="{DynamicResource 菜单按钮样式}"/>
                    <Button x:Name="BtnAbout" Content="关于"  Style="{DynamicResource 菜单按钮样式}"/>
                </StackPanel>
                <WrapPanel Orientation="Vertical">
                    <WrapPanel  x:Name="BtnsFile" Orientation="Vertical">
                        <Button x:Name="BtnOpen" Content="{DynamicResource 打开}" Style="{DynamicResource 工具按钮样式}"/>
                        <Button x:Name="BtnAdd" Content="{DynamicResource 添加}" Style="{DynamicResource 工具按钮样式}"/>
                        <Button x:Name="BtnRemove" Content="{DynamicResource 删除}" Style="{DynamicResource 工具按钮样式}"/>
                    </WrapPanel>
                    <WrapPanel  x:Name="BtnsSetting" Orientation="Vertical" Visibility="Collapsed">
                        <Button x:Name="BtnReset" Content="{DynamicResource 重置}" ToolTip="{DynamicResource 重置}" Style="{DynamicResource 工具按钮样式}"/>
                        <Button x:Name="BtnApply" Content="{DynamicResource 应用}" ToolTip="{DynamicResource 应用}" Style="{DynamicResource 工具按钮样式}"/>
                        <Button x:Name="BtnClearRunInfo" Content="{DynamicResource 清空}" ToolTip="{DynamicResource 清空+}" Style="{DynamicResource 工具按钮样式}"/>
                    </WrapPanel>
                    <WrapPanel  x:Name="BtnsAbout" Orientation="Vertical" Visibility="Collapsed">
                        <Button x:Name="BtnHomePage" Content="{DynamicResource 官网}" ToolTip="{DynamicResource 官网+}" Style="{DynamicResource 工具按钮样式}"/>
                        <Button x:Name="BtnInfoPage" Content="{DynamicResource 主页}" ToolTip="{DynamicResource 主页+}" Style="{DynamicResource 工具按钮样式}"/>
                        <Button x:Name="BtnDownloadPage" Content="{DynamicResource 下载}" ToolTip="{DynamicResource 下载+}" Style="{DynamicResource 工具按钮样式}"/>
                        <Button x:Name="BtnFeedbackPage" Content="{DynamicResource 反馈}" ToolTip="{DynamicResource 反馈+}" Style="{DynamicResource 工具按钮样式}"/>
                        <Button x:Name="BtnGitHubPage" Content="{DynamicResource 开源}" ToolTip="{DynamicResource 开源+}" Style="{DynamicResource 工具按钮样式}"/>
                        <Button x:Name="BtnQQGroup" Content="{DynamicResource 讨论}" ToolTip="{DynamicResource 讨论+}" Style="{DynamicResource 工具按钮样式}"/>
                    </WrapPanel>
                </WrapPanel>
            </DockPanel>
            <!--中-->
            <Grid x:Name="PlayList" Background="#FFE6E6E6" Margin="0" Width="240">
                <DockPanel>
                    <WrapPanel Style="{DynamicResource 水平流式面板样式}" ItemWidth="80" Margin="0" DockPanel.Dock="Top">
                        <Button x:Name="ButtonAdd" Content="{DynamicResource 添加}" Style="{DynamicResource 自由按钮样式}" Click="MenuAdd_Click" Padding="0"/>
                        <Button x:Name="ButtonDelete" Content="{DynamicResource 删除}" Style="{DynamicResource 自由按钮样式}" Click="MenuDelete_Click" Padding="0"/>
                        <Button x:Name="ButtonPlayMode" Content="{DynamicResource 单次}" Style="{DynamicResource 自由按钮样式}" Click="ButtonPlayMode_Click" />
                    </WrapPanel>
                    <ListBox x:Name="PlayListBox" Background="White" Margin="0" Drop="PlayListBox_Drop" 
                             SizeChanged="PlayListBox_SizeChanged" PreviewMouseMove="PlayListBox_PreviewMouseMove" PreviewDrop="PlayListBox_PreviewDrop" PreviewMouseLeftButtonDown="PlayListBox_PreviewMouseLeftButtonDown"/>
                </DockPanel>

                <DockPanel x:Name="CenterAboutPage" Visibility="Collapsed">
                    <StackPanel DockPanel.Dock="Top">
                        <Label Content="{DynamicResource 名称}" Style="{DynamicResource 标题标签样式}"/>
                        <TextBlock x:Name="ThisName" Text="?" Style="{DynamicResource 文本显示框样式}"/>
                        <Label Content="{DynamicResource 说明}" Style="{DynamicResource 标题标签样式}"/>
                        <TextBlock x:Name="Description" Text="?" Style="{DynamicResource 文本显示框样式}"/>
                        <Label Content="{DynamicResource 作者}" Style="{DynamicResource 标题标签样式}"/>
                        <TextBlock x:Name="Developer" Text="?" Style="{DynamicResource 文本显示框样式}"/>
                        <Label Content="{DynamicResource 版本}"  Style="{DynamicResource 标题标签样式}"/>
                        <TextBlock x:Name="Version" Text="?" Style="{DynamicResource 文本显示框样式}"/>
                        <Label Content="{DynamicResource 赞助}"  Style="{DynamicResource 标题标签样式}"/>
                        <TextBlock x:Name="BitCoinAddress" Text="?" ToolTip="{DynamicResource 点复}" Style="{DynamicResource 文本显示框样式}" Cursor="Hand" FontSize="10" MouseLeftButtonDown="BitCoinAddress_MouseLeftButtonDown"/>
                        <Label Content="{DynamicResource 日志}" Style="{DynamicResource 标题标签样式}"/>
                    </StackPanel>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBlock x:Name="UpdateNote" Text="?" Style="{DynamicResource 文本显示框样式}" TextWrapping="Wrap" VerticalAlignment="Top"/>
                    </ScrollViewer>
                </DockPanel>
            </Grid>
            <!--右-->
            <Grid x:Name="Player" Background="Black" Drop="Player_Drop" MouseDown="Player_MouseDown">
                <MediaElement x:Name="Media" LoadedBehavior="Manual" Volume="0.5" AllowDrop="True" MediaOpened="MediaPlayer_MediaOpened" MediaEnded="Media_MediaEnded" MouseLeftButtonUp="Media_MouseLeftButtonUp" />
                <Image x:Name="Cover" Margin="0" MouseLeftButtonUp="Media_MouseLeftButtonUp"/>
                <Label x:Name="Message" Content="消息提示" Margin="10,10,10,0" Foreground="#FFFFE800" Background="{x:Null}" Height="30" VerticalAlignment="Top" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="14" Padding="3,0,0,0"/>
                <Grid x:Name="ConsoleGrid" Height="125" VerticalAlignment="Bottom" Background="#7FFFFFFF" AllowDrop="False" MouseEnter="ConsoleGrid_MouseEnter" MouseLeave="ConsoleGrid_MouseLeave" DockPanel.Dock="Bottom">
                    <Label x:Name="VideoName" Content="{DynamicResource 媒体路径}" Margin="10,5,160,0" Foreground="Black" ToolTip="{DynamicResource 媒体路径}" Height="25" VerticalAlignment="Top"/>
                    <Label x:Name="VideoVolume" Content="{DynamicResource 音量}" Margin="0,5,110,0" Foreground="Black" HorizontalAlignment="Right" Height="25" VerticalAlignment="Top"/>
                    <Slider x:Name="VolumeSlider" IsMoveToPointEnabled="True" HorizontalAlignment="Right" Margin="0,8,10,0" 
                    Width="100" SmallChange="0" Value="0.5" Cursor="Hand" LargeChange="0" ToolTip="0.5" TickFrequency="10" Maximum="1" Focusable="False" Height="22" VerticalAlignment="Top" IsTabStop="False"/>
                    <Slider x:Name="TimeSlider" IsMoveToPointEnabled="True" Margin="10,30,10,0" Height="23" VerticalAlignment="Top"
                     Loaded="TimeSlider_Loaded" Maximum="1" LargeChange="10" SmallChange="5" Cursor="Hand" Focusable="False" AllowDrop="False" IsSelectionRangeEnabled="True" />
                    <Label x:Name="TimePosition" Content="00:00:00" Margin="10,53,0,0" Foreground="Black" HorizontalAlignment="Left" Width="58" Height="25" VerticalAlignment="Top" AllowDrop="False"/>
                    <Label x:Name="TimeAll" Content="00:00:00" Margin="0,53,10,0" VerticalAlignment="Top" Foreground="Black" HorizontalAlignment="Right" Width="58" AllowDrop="False"/>
                    <Button x:Name="ButtonOpen" Content="{DynamicResource 打开}" Click="MenuOpen_Click" Height="25" RenderTransformOrigin="0.833,0.52" HorizontalAlignment="Left" Width="60" Margin="10,0,0,10" VerticalAlignment="Bottom" AllowDrop="False"/>
                    <Grid HorizontalAlignment="Center" Margin="75,0" Width="342" Height="67" VerticalAlignment="Bottom">
                        <Button x:Name="ButtonReplay" Content="{DynamicResource 重播}" Click="MenuReplay_Click" Height="25" RenderTransformOrigin="0.638,0.44" HorizontalAlignment="Center" Width="60" VerticalAlignment="Bottom" Margin="6,0,276,40" AllowDrop="False" />
                        <Button x:Name="ButtonBack" Content="{DynamicResource 快退}" Click="MenuBack_Click" Height="25" RenderTransformOrigin="1.067,0.48" HorizontalContentAlignment="Center" HorizontalAlignment="Center" Width="60" VerticalAlignment="Bottom" Margin="71,0,211,40" AllowDrop="False" />
                        <Button x:Name="ButtonPlay" Content="{DynamicResource 播放}" Click="MenuPlay_Click" Height="25" Width="70" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="96,0,96,40" />
                        <Button x:Name="ButtonForward" Content="{DynamicResource 快进}" Click="MenuForward_Click" Height="25" HorizontalAlignment="Center" Width="60" VerticalAlignment="Bottom" Margin="211,0,71,40" RenderTransformOrigin="1.329,0.48" AllowDrop="False" />
                        <Button x:Name="ButtonLRFlip" Content="{DynamicResource 左右翻转}" Click="MenuLRFlip_Click" Height="25" RenderTransformOrigin="1.067,0.48" HorizontalContentAlignment="Center" HorizontalAlignment="Center" Width="60" VerticalAlignment="Bottom" Margin="276,0,6,40" AllowDrop="False" />
                        <Button x:Name="ButtonSetLoopStart" Content="{DynamicResource 循环起点}" Click="MenuLoopStart_Click" Height="25" RenderTransformOrigin="1.067,0.48" HorizontalContentAlignment="Center" HorizontalAlignment="Center" Width="70" VerticalAlignment="Bottom" Margin="25,0,247,10" AllowDrop="False" />
                        <Button x:Name="ButtonSetLoopEnd" Content="{DynamicResource 循环终点}" Click="MenuLoopEnd_Click" Height="25" HorizontalAlignment="Center" Width="70" VerticalAlignment="Bottom" Margin="100,0,172,10" RenderTransformOrigin="1.329,0.48" AllowDrop="False" />
                        <Button x:Name="ButtonClearLoop" Content="{DynamicResource 清除循环}" Click="MenuClearLoop_Click" Height="25" HorizontalAlignment="Center" Width="70" VerticalAlignment="Bottom" Margin="175,0,97,10" RenderTransformOrigin="1.329,0.48" AllowDrop="False" />
                        <Button x:Name="ButtonList" Content="{DynamicResource 播放列表}" Click="MenuHideList_Click" HorizontalAlignment="Center" Width="70" Height="25" VerticalAlignment="Bottom" Margin="250,0,22,10" AllowDrop="False"/>
                    </Grid>
                    <Button x:Name="ButtonHelp" Content="{DynamicResource 帮助}" Click="MenuHelp_Click" Background="#FF646464" Foreground="White" HorizontalAlignment="Right" Width="60" RenderTransformOrigin="1.767,0.56" Margin="0,0,10,10" Height="25" VerticalAlignment="Bottom" AllowDrop="False"/>
                </Grid>
            </Grid>
        </DockPanel>
    </Grid>
</Window>